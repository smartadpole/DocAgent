# 服务实例台账条目模板

使用位置：[[projects/service-registry]]

```md
## <service-id>

- `service_id`：
- `host`：
- `endpoint`：
- `service_name`：
- `components`：
- `owner`：
- `code_path_key`：
- `git_remote`：
- `git_revision`：
- `start_command`：
- `config_profile`：
- `data_root_key`：
- `status_store`：
- `log_path_key`：
- `health_check`：
- `preflight`：
- `update_method`：
- `last_verified_at`：
- `notes`：
```

只写可长期复用的运行事实和脱敏后的配置键，不写真实密钥、临时进程号或一次性排障流水。
