---
type: template
id: TEMPLATE-GOAL-CONTRACT-001
status: active
updated: 2026-05-25
tags: [template, codex, goal, harness]
---

# Goal Contract 模板

用于把长时任务写成可执行、可审计的完成契约。适合终点明确、路径需要在执行中探索的任务。

## 基本信息

- **目标 ID / 任务 ID**：
- **适用范围**：主控文档库 / 子工程 / 跨工程联调 / 研究复现 / 其他
- **Owner**：
- **当前响应模式**：快速诊断 / 知识沉淀 / Issue 分析 + 沉淀 / 验收关闭 / 规则升级 / 子工程实现 / 回传
- **是否适合 `/goal`**：是 / 否 / 仅作为人工目标契约

## 完成契约

- **期望最终状态**：
- **验证面**：测试 / benchmark / 报告 / artifact / DB readback / 日志 / UI / 论文材料 / 其他
- **必须保持的约束**：
- **允许使用的文件 / 工具 / 数据 / 环境**：
- **不做项**：

## 迭代策略

- **每轮检查什么**：
- **每轮记录什么**：
- **下一步选择规则**：
- **预算或时间边界**：

## 阻塞停止条件

- **必须停止并汇报的条件**：
- **阻塞时必须报告的证据**：
- **解锁下一步需要的输入**：

## 主控 / 子工程分工

- **主控定义**：结果、验收口径、状态关闭条件、风险归口
- **子工程执行**：实现范围、本地验证、失败项、未验证边界、回传证据
- **主控吸收入口**：
- **子工程回传入口**：

## `/goal` 草稿

```text
/goal <期望最终状态>，由 <验证面> 验证，同时保持 <约束>。只使用 <边界>。每次迭代之间，记录 <改动、证据变化、下一步选择依据>。如果 <阻塞停止条件>，停止并报告 <已尝试路径、已收集证据、阻塞点、所需输入>。
```
